name: 'Perform SAST scan'
description: 'Perform a SAST scan on Fortify on Demand'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: patricioperezv/boaboa-fortify-action/internal/set-fod-var-defaults@v1.2.2
    - uses: patricioperezv/boaboa-fortify-action/setup@v1.2.2
      with:
        export-path: false
        fcli: action-default
    - uses: patricioperezv/boaboa-fortify-action/internal/fod-login@v1.2.2
    - uses: patricioperezv/boaboa-fortify-action/package@v1.2.2
    - uses: patricioperezv/boaboa-fortify-action/internal/run@v1.2.2
      with:
        cmd: '"${FCLI_CMD}" fod sast-scan start --rel "${FOD_RELEASE}" -f package.zip --store fod_scan ${EXTRA_FOD_SAST_SCAN_OPTS}'
    - uses: patricioperezv/boaboa-fortify-action/internal/run@v1.2.2
      if: env.DO_WAIT == 'true' || env.DO_EXPORT == 'true'
      with:
        cmd: '"${FCLI_CMD}" fod sast-scan wait-for ::fod_scan::'
    - uses: patricioperezv/boaboa-fortify-action/internal/fod-logout@v1.2.2
    - if: env.DO_EXPORT == 'true'
      uses: patricioperezv/boaboa-fortify-action/fod-export@v1.2.2
        
branding:
  icon: 'shield'
  color: 'blue'

